import React, { useEffect, useState } from "react";

function authenticate(children: React.ReactNode) {
  const [authenticated, setAuthenticated] = useState(false);

  useEffect(() => {



  },[])

  return <>{children}</>;
}

export default authenticate;
